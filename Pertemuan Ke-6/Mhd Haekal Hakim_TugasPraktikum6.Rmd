---
title: "Mhd Haekal Hakim_Tugas Praktikum 7"
author: "Mhd Haekal Hakim"
date: "2025-10-02"
output: html_document
---

## Library

```{r warning=FALSE}
library(tseries)
library(ggplot2)
library(tsibble)
library(MASS)
library(readxl)
library(forecast)
library(TSA)
library(TTR)
library(aTSA)
library(graphics)
```

## Data

```{r warning=FALSE}
## Baca Data
data <- read_excel("C:/Users/mhdha/OneDrive/Dokumen/IPB/Semester 5/MPDW/JKSE_Close_Weekly.xlsx")

data1 <- data[518:775, ]
data1
```

### Mengambil Data Train

```{r}
n <- nrow(data1)              # jumlah data = 258
n_train <- floor(0.8 * n)  # train = 206
dt_rg <- data1$`Close`[1:n_train] |> ts()
```

### Membuat Plot Data Time Series

```{r}
dt_rg |> as_tsibble() |> 
  ggplot(aes(x = index, y = value)) +
  geom_line() + theme_bw() +
  xlab("Obs") + ylab("Nilai")
```

Berdasarkan plot time series tersebut, terlihat bahwa data tidak stasioner dalam rataan, ditandai dengan adanya trend pada data dan tidak stasioner dalam ragam, ditandai dengan adanya perbedaan lebar pita pada plot

#### Uji ACF

```{r}
acf(dt_rg)
```

Berdasarkan plot ACF, terlihat bahwa plot ACF pada data tersebut menurun secara perlahan (*tails off slowly*) yang menandakan data tidak stasioner dalam rataan

#### Uji ADF

```{r}
adf.test(dt_rg)
```

$H_0$: Data tidak stasioner dalam rataan

$H_1$: Data stasioner dalam rataan

Berdasarkan uji ADF tersebut, didapat p-value sebesar 0.5002 yang lebih besar dari taraf nyata 5% dan menandakan bahwa data tidak stasioner dalam rataan. Hal ini sesuai dengan hasil eksplorasi menggunakan plot time series dan plot ACF.

### Uji Box-cox

```{r}
# Bikin Index
index <- seq_along(dt_rg)
```

```{r}
# Buat Data Supaya Positif
shift_val <- abs(min(dt_rg)) + 1
dt_rg_shift <- dt_rg + shift_val
```

```{r}
# Regresi Linear untuk Box-Cox
lm_bc <- lm(dt_rg_shift ~ index)
```

```{r}
# Uji Box-Cox
bc <- boxcox(lm_bc, lambda = seq(0, 5, by = 0.001))
```

```{r}
# Cari Lambda Optimum
lambda_opt <- bc$x[which.max(bc$y)]
ci_lambda <- bc$x[bc$y > max(bc$y) - 0.5 * qchisq(0.95, 1)]

cat("Lambda Optimum:", lambda_opt, "\n")
cat("95% CI Lambda:", min(ci_lambda), "-", max(ci_lambda), "\n")

```
### Transformasi
```{r}
# --- Transformasi Box-Cox ---
# Pastikan semua nilai positif
shift_val <- abs(min(dt_rg)) + 1
dt_shift <- dt_rg + shift_val

# Regresi linear untuk uji Box-Cox
lm_bc <- lm(dt_shift ~ seq_along(dt_shift))

# Uji Box-Cox dengan rentang lambda yang cukup luas
bc <- boxcox(lm_bc, lambda = seq(-1, 2, 0.001))

# Ambil lambda optimum
lambda_opt <- bc$x[which.max(bc$y)]
cat("Lambda optimum Box-Cox:", lambda_opt, "\n")

# Transformasi Box-Cox
dt_bc <- BoxCox(dt_shift, lambda = lambda_opt)

# Plot hasil transformasi
dt_bc |> as_tsibble() |> 
  ggplot(aes(x = index, y = value)) +
  geom_line() + theme_bw() +
  xlab("Obs") + ylab("Box-Cox transformed")
```

### Differencing

```{r}
# Differencing setelah transformasi
dt_diff <- diff(dt_bc, differences = 1)

# Plot
dt_diff |> as_tsibble() |> 
  ggplot(aes(x = index, y = value)) + 
  geom_line() + theme_bw() +
  xlab("Obs") + ylab("Differenced (BoxCox)")


```

#### Cek ACF dan ADF

```{r}
acf(dt_diff)
```

Berdasarkan plot ACF, terlihat bahwa plot ACF pada data tersebut berada dalam rentang yang menandakan bahwa data stasioner dalam rataan

```{r warning=FALSE}
adf.test(dt_diff)
```

$H_0$: Data tidak stasioner dalam rataan

$H_1$: Data stasioner dalam rataan

Berdasarkan uji ADF tersebut, didapat p-value sebesar 0.01 yang lebih kecil dari taraf nyata 5% dan menandakan bahwa data stasioner dalam rataan. Hal ini sesuai dengan hasil eksplorasi menggunakan plot time series dan plot ACF.

## Spesifikasi Model
```{r}
par(mfrow = c(1,2))
```

```{r}
acf(dt_diff, main ="ACF", lag.max=20)
```

```{r}
pacf(dt_diff, main="PACF", lag.max=20)
```

```{r}
par(mfrow = c(1,1))
```

Berdasarkan Plot ACF, terlihat cuts off pada lag ke-1 sehingga dapat kita asumsikan model yang terbentuk adalah ARIMA(0,0,1). Selanjutnya, berdasarkan plot PACF, tidak terlihat cuts off pada lag pertama sehingga model yang terbentuk adalah ARIMA(0,0,0). Selain dengan plot ACF dan PACF, penentuan spesifikasi model dilakukan dengan extended ACF (EACF) berikut ini.

```{r}
eacf(dt_diff)
```
Menggunakan plot EACF, dapat diambil beberapa model dengan melihat ujung segitiga yang terbentuk, antara lain ARIMA(0,0,0), ARIMA(1,0,1), ARIMA(2,0,2), ARIMA(3,0,3), ARIMA(4,0,4), dan ARIMA(6,0,6).

## Pendugaan Parameter
```{r}
model1=Arima(dt_diff, order=c(0,0,0),method="ML")
summary(model1)
```

```{r}
lmtest::coeftest(model1)
```

```{r}
model2=Arima(dt_diff, order=c(0,0,1),method="ML")
summary(model2)
```

```{r}
lmtest::coeftest(model2)
```

```{r}
model3=Arima(dt_diff, order=c(1,0,1),method="ML")
summary(model3)
```

```{r}
lmtest::coeftest(model3) #seluruh parameter signifikan
```

```{r}
model4=Arima(dt_diff, order=c(3,0,3),method="ML")
summary(model4)
```

```{r}
lmtest::coeftest(model4)
```
```{r}
model5=Arima(dt_diff, order=c(4,0,4),method="ML")
summary(model5)
```

```{r}
lmtest::coeftest(model5)
```

```{r}
model6=Arima(dt_diff, order=c(6,0,6),method="ML")
summary(model6)
```

```{r}
lmtest::coeftest(model6)
```
```{r}
model7=Arima(dt_diff, order=c(2,0,2),method="ML")
summary(model7)
```

```{r}
lmtest::coeftest(model7)
```
### Pemilihan Model Terbaik
```{r}
hasil_model <- data.frame(
  Model = c("ARIMA(0,0,0)",
            "ARIMA(0,0,1)",
            "ARIMA(1,0,1)",
            "ARIMA(3,0,3)",
            "ARIMA(4,0,4)",
            "ARIMA(6,0,6)",
            "ARIMA(2,0,2)"),
  AIC = c(
    AIC(model1),
    AIC(model2),
    AIC(model3),
    AIC(model4),
    AIC(model5),
    AIC(model6),
    AIC(model7)
  ),
  BIC = c(
    BIC(model1),
    BIC(model2),
    BIC(model3),
    BIC(model4),
    BIC(model5),
    BIC(model6),
    BIC(model7)
  )
)

hasil_model
```
Berdasarkan pendugaan parameter di atas, nilai AIC terkecil dimiliki oleh model ARIMA(0,0,1) dan parameter model ARIMA(0,0,1) juga signifikan pada ma1 tetapi tidak signifikan pada intercept nya. Tetapi model ARIMA(0,0,1) yang terbaik dibandingkan yang lainnya.

## Analisis Sisaan
### Eksplorasi Sisaan
```{r}
#Eksplorasi
sisaan <- model1$residuals
par(mfrow=c(2,2))
```

```{r}
#qqnorm(sisaan)
#qqline(sisaan, col = "blue", lwd = 2)
car::qqPlot(sisaan)
```

```{r}
plot(c(1:length(sisaan)),sisaan)

# ACFF dan PACF sisaan
acf(sisaan)
pacf(sisaan)
```

```{r}
par(mfrow = c(2,2))
```
Berdasarkan plot kuantil-kuantil normal, secara eksplorasi ditunjukkan sisaan menyebar normal mengikuti garis $45^o$. Kemudian dapat dilihat juga lebar pita sisaan yang cenderung sama menandakan bahwa sisaan memiliki ragam yang homogen. Akan tetapi, plot ACF dan PACF sisaan ARIMA(0,0,1) tidak ada yang signifikan pada lag sehingga sisaan saling bebas. Kondisi ini akan diuji lebih lanjut dengan uji formal.

### Uji Formal
```{r}
#1) Sisaan Menyebar Normal
ks.test(sisaan,"pnorm") 
```
Selain dengan eksplorasi, asumsi tersebut dapat diuji menggunakan uji formal. Pada tahapan ini uji formal yang digunakan untuk normalitas adalah uji Kolmogorov-Smirnov (KS). Hipotesis pada uji KS adalah sebagai berikut.

$H_0$: Sisaan menyebar normal

$H_1$: Sisaan tidak menyebar normal

Berdasarkan uji KS tersebut, didapat p-value < 0.0.5 yang lebih kecil dari taraf nyata 5% sehingga tolak $H_0$ dan menandakan bahwa sisaan menyebar tidak normal. Hal ini sesuai dengan hasil eksplorasi menggunakan plot kuantil-kuantil normal.

```{r}
#2) Sisaan saling bebas/tidak ada autokorelasi
Box.test(sisaan, type = "Ljung") 
```
Selanjutnya akan dilakukan uji formal untuk kebebasan sisaan menggunakan uji Ljung-Box. Hipotesis yang digunakan adalah sebagai berikut.

$H_0$: Sisaan saling bebas

$H_1$: Sisaan tidak saling bebas

Berdasarkan uji Ljung-Box tersebut, didapat p-value sebesar 0.05474 yang lebih besar dari taraf nyata 5% sehingga gagal tolak $H_0$ dan menandakan bahwa sisaan saling bebas. Hal ini berbeda dengan eksplorasi.

```{r}
#3) Sisaan homogen
Box.test((sisaan)^2, type = "Ljung") 
```
Hipotesis yang digunakan untuk uji kehomogenan ragam adalah sebagai berikut.

$H_0$: Ragam sisaan homogen

$H_1$: Ragam sisaan tidak homogen

Berdasarkan uji Ljung-Box terhadap sisaan kuadrat tersebut, didapat p-value sebesar 0.1149 yang lebih besar dari taraf nyata 5% sehingga gagal tolak $H_0$ dan menandakan bahwa ragam sisaan homogen.

```{r}
#4) Nilai tengah sisaan sama dengan nol
t.test(sisaan, mu = 0, conf.level = 0.95) 
```
Terakhir, dengan uji-t, akan dicek apakah nilai tengah sisaan sama dengan nol. Hipotesis yang diujikan sebagai berikut.

$H_0$: Nilai tengah sisaan sama dengan 0

$H_1$: Nilai tengah sisaan tidak sama dengan 0

Berdasarkan uji-t tersebut, didapat p-value sebesar 1 yang lebih besar dari taraf nyata 5% sehingga gagal tolak $H_0$ dan menandakan bahwa nilai tengah sisaan sama dengan nol. Hal ini berbeda dengan eksplorasi.

Hanya normalitas sisaan yang gagal, model ARIMA masih boleh dipakai untuk forecasting

## Overfitting
```{r}
model2=Arima(dt_diff, order=c(0,0,1),method="ML")
summary(model2)
```

```{r}
lmtest::coeftest(model2)
```

## Model Terbaik
```{r}
model2=Arima(dt_diff, order=c(0,0,1),method="ML")
summary(model2)
```

```{r}
lmtest::coeftest(model2)
```

## Peramalan

```{r}
# train.ts: data training pada skala original (sebelum differencing)
train.ts <- dt_rg   # dt_rg kamu sudah berisi train original

# test.ts: data uji (sisa setelah n_train)
test.ts <- data1$`Close`[(n_train + 1):n] |> ts()
```

```{r}
ramalan <- forecast::forecast(model2, h = length(test.ts))
```

```{r}
data.ramalan <- ramalan$mean
plot(ramalan)
```

```{r}
# Forecast pada data yang sudah di-differencing (model2)
ramalan <- forecast::forecast(model2, h = length(test.ts))
data.ramalan <- as.numeric(ramalan$mean)
plot(ramalan)

# --- Inverse differencing ---
# xi_value adalah nilai terakhir dari data sebelum differencing (yaitu dt_bc)
xi_value <- as.numeric(tail(dt_bc, 1))  # bukan train.ts

forecast_inv <- diffinv(data.ramalan, differences = 1, xi = xi_value)

# diffinv hasilnya punya panjang +1, jadi buang elemen pertama
hasil_diffinv <- as.numeric(forecast_inv[-1])

# Pastikan panjang sesuai dengan test set
hasil_diffinv <- hasil_diffinv[seq_len(length(test.ts))]

# --- Inverse Box-Cox ---
hasil_invbc <- InvBoxCox(hasil_diffinv, lambda = lambda_opt)

# --- Kembalikan ke skala asli ---
hasil_final <- hasil_invbc - shift_val

# 🔹 Bandingkan aktual vs hasil ramalan
perbandingan <- data.frame(
  Aktual   = as.numeric(test.ts),
  Forecast = hasil_final
)

print(head(perbandingan, 10))

# 🔹 Hitung akurasi
library(Metrics)
rmse_val <- sqrt(mean((perbandingan$Aktual - perbandingan$Forecast)^2, na.rm = TRUE))
mape_val <- mean(abs((perbandingan$Aktual - perbandingan$Forecast) / perbandingan$Aktual), na.rm = TRUE) * 100

cat("RMSE:", rmse_val, "  MAPE (%):", mape_val, "\n")
```

